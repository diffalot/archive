require File.dirname(__FILE__)+'/../spec_helper'

describe Archive::PayloadFile do
  before(:each) do
    @payload_file = Archive::PayloadFile.new
  end

  describe "when stuff" do
    before(:each) do
    end
  end
end
