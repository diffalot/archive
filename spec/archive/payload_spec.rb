require File.dirname(__FILE__)+'/../spec_helper'

describe Archive::Payload do
  before(:each) do
    @payload = Archive::Payload.new
  end

  describe "when stuff" do
    before(:each) do
    end
  end
end
